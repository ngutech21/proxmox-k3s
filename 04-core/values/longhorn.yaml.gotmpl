defaultSettings:
  defaultDataPath: /var/lib/longhorn
  defaultReplicaCount: 3

longhornUI:
  replicas: 3

ingress:
  enabled: true
  ingressClassName: traefik
  host: {{ .Values.cluster.longhornHost | quote }}
  tls: false
