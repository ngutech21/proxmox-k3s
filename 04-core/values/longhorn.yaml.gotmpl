defaultSettings:
  defaultDataPath: /var/lib/longhorn
  defaultReplicaCount: 3

longhornUI:
  replicas: 3

ingress:
  enabled: true
  ingressClassName: traefik
  host: {{ env "LONGHORN_HOST" | default "longhorn.k3s.home" | quote }}
  tls: false
